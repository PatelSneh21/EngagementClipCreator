"""Build candidate segments from transcript + scenes."""

from __future__ import annotations

from pathlib import Path


def build_candidate_segments(transcript_path: Path, scene_path: Path) -> list[dict]:
    """TODO: stitch transcript and scene data into candidates."""
    raise NotImplementedError("Candidate builder not implemented yet.")
